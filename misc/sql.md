SELECT 
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH,
    IS_NULLABLE
FROM 
    INFORMATION_SCHEMA.COLUMNS
WHERE 
    TABLE_SCHEMA = 'achats'
    AND TABLE_NAME = 'TicketEntetes'
ORDER BY 
    ORDINAL_POSITION;